import pyttsx3

engine = pyttsx3.init()
engine.say("I will speak this text")
engine.runAndWait()
#Set the rate of speech

# Open the CSV file and read data into a list of dictionaries


# Loop through the data and perform actions

    # Speak word

    # Prompt user for choice

    # Speak definition if chosen

    # Speak sentence if chosen

    # Prompt user to spell the word if chosen

#Say definition

#Say spelling word again

#Prompt string input to spell the word (caps, hyphens, spacing)

#If correct, next word

#If incorrect, ask to spell again and record the term to a text file
